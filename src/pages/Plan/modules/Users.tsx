import { useModule } from 'src/features/modules/useModule';

const Users = () => {
  const { value, setOutput, remove } = useModule('title');

  return <>Module Users not implemented yet</>;
};

export default Users;
