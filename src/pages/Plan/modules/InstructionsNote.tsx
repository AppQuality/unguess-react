import { useModule } from 'src/features/modules/useModule';

const InstructionsNote = () => {
  const { value, setOutput, remove } = useModule('title');

  return <>Module InstructionsNote not implemented yet</>;
};

export default InstructionsNote;
